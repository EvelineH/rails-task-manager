<h2><%= @task.title %></h2>
<p><%= @task.details %></p>

<div>
    <input type="checkbox" title="<%= @task.title.downcase %></>" />
    <label for="<%= @task.title.downcase %>">
    <%= @task.completed ? 'This task is completed' : 'This task is not completed yet' %></></label>
</div>
<p>

<p><%= link_to 'Back to list', tasks_path %></p>
